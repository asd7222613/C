// purpose: all variables must be declared before use
#include <stdio.h>

int main(int argc, char const *argv[])
{
    // uninitial variables maybe random value
    int lower, upper, step;
    char c, line[1000];

    printf("init lower: %d, upper: %d, step: %d\n", lower, upper, step);

    return 0;
}
